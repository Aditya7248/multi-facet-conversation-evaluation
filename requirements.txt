# Core
pydantic>=2.6
pyyaml>=6.0
pandas>=2.1
numpy>=1.26
tqdm>=4.66
rich>=13.7
typer>=0.12
python-dotenv>=1.0
tenacity>=8.2

# Embeddings + ANN index (facet routing)
sentence-transformers>=2.7
faiss-cpu>=1.8

# LLM client transports
httpx>=0.27

# API + UI
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.33
altair>=5.2

# Tests + lint (kept here so CI has them; move to dev-only if you prefer)
pytest>=8.1
pytest-cov>=5.0
ruff>=0.4
